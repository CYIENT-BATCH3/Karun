//Algorithm for below program:
1.program to find IVRS functionality access music-function using functions in c.
2.declare music as a function in global.
3.write calling function in main.
4.define the music functtion to access.
5.write the conditions for choosing options


#include<stdio.h>
void music();	//function declaration
void main()     //main function
{
   int k;       //declaring variable
   printf("enter key"); 
   scanf("%d",&k);      //reading the variable
   music(k);            //calling function
    }
void music(int number){      //defining the function
    if(number==1)            //conditions for choosing number
   printf("music is playing");
   else if(number==2)
   printf("music is stopped");
   else if(number==3)
   printf("music is stopped");
   else if(number==4)
   printf("current song is..........");
   else if(number==5)
   printf("list of songs are:\n1....\n2....\n3....");
   else
 printf("\ninvalid");
 }
