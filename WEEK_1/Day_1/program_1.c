*WAP uses atleast 5 keywords in c and print some output using same?

#include <stdio.h>

int main()
{
   int a;
   char b;
   float c;
   double d;
   printf("%d\n%d\n%d\n%d\n",sizeof(a),sizeof(b),sizeof(c),sizeof(d));   //printing the sizeof all the datatypes
    return 0;
}
